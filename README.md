# Myblog
A blog application that includes user registration, supports user posts, a user profile page, allows for followers, and email notifications. This project was built using Python with Flask for the backend and HTML/JINJA2/Bootstrap for the frontend. It includes a SQLite database and uses SQLAlchemy to communicate with the database.
